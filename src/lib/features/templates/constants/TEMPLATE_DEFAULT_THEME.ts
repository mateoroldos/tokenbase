import type { Theme } from '$lib/features/token-groups-store/types/design-system-overview.interface'

const TEMPLATE_DEFAULT_THEME: Theme = {
	id: 'default',
	name: 'Default'
}

export default TEMPLATE_DEFAULT_THEME
