import type { Template } from '../../types/template-interface'

export const overview: Template = {
	name: 'Font weight tokens template',
	description:
		'Font weight tokens provide a structured approach to managing text styles.',
	tags: ['official'],
	type: 'group'
}
