import type { Template } from '../../types/template-interface'

export const overview: Template = {
	name: 'Material design color template',
	description: 'All color variables from material design.',
	tags: ['colors'],
	type: 'group'
}
