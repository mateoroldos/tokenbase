import type { Template } from '../../types/template-interface'

export const overview: Template = {
	name: 'Number tokens template',
	description:
		'Number tokens provide a structured way to handle numerical values in your design.',
	tags: ['official'],
	type: 'group'
}
