import type { Template } from '../../types/template-interface'

export const overview: Template = {
	name: 'Tailwind Colors',
	description: 'Color palettes from Tailwind CSS.',
	tags: ['tailwind', 'colors'],
	type: 'groups'
}
